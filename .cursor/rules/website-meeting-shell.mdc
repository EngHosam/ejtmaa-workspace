---
description: Organization-host Meeting shell IA — role drawer tiles, wide live tile, Meeting info after live, STARTED broadcast + frosted MeetingPageOverlay, FlexContainer content column, header identity, in-shell MeetingPage (incl. agenda, talk queue)
globs:
  - website/src/app/ui/components/meeting/MeetingDrawerPanel.tsx
  - website/src/app/ui/components/meeting/MeetingHeader.tsx
  - website/src/app/ui/components/meeting/MeetingHeaderMe.tsx
  - website/src/app/ui/components/meeting/MeetingAttendanceCard.tsx
  - website/src/app/ui/components/meeting/MeetingLiveBroadcast.tsx
  - website/src/app/ui/components/meeting/MeetingPageOverlay.tsx
  - website/src/app/ui/components/meeting/MeetingPrimaryButton.tsx
  - website/src/app/ui/components/meeting/MeetingMetaChip.tsx
  - website/src/app/ui/components/meeting/hooks/useMeetingAttendance.ts
  - website/src/app/ui/components/FilterCountChips.tsx
  - website/src/app/ui/components/meeting/MeetingDrawerOverlay.tsx
  - website/src/app/ui/components/meeting/MeetingAgendaCard.tsx
  - website/src/app/ui/components/meeting/hooks/useMeetingAgenda.ts
  - website/src/app/ui/components/meeting/MeetingTalkQueueCard.tsx
  - website/src/app/ui/components/meeting/hooks/useMeetingTalkQueue.ts
  - website/src/app/ui/components/meeting/pages/**
  - website/src/app/ui/components/meeting/hooks/useMeetingPage.tsx
  - website/src/app/ui/components/meeting/hooks/useOrganization.ts
  - website/src/app/ui/components/HomeMark.tsx
  - website/src/app/ui/layouts/MeetingLayout.tsx
  - website/src/app/ui/pages/Meeting.tsx
alwaysApply: false
---

# Website Meeting shell (org host)

Authority: `docs/platforms/website/organization-host-routing.md` §5.4–§5.5.
Linking gate / session surface: `.cursor/rules/website-meeting-live-session.mdc`.
Participant type permissions: `docs/platforms/backend/contracts/meeting-participant-domain.md` §8.
Enum display labels: `.cursor/rules/website-backend-enum-label-mirror.mdc`.

## When the shell mounts

`MeetingShell` renders header / drawer / footer / children only when `linking === "READY"`. Do not assume drawer/header run during PENDING or FAILED.

READY main column: wrap route children in `FlexContainer` (`dir="column"`, `flx_1`, `minH={0}`) so page / broadcast / overlay width matches header & footer `Container` (`defaultContainerSizeDims`). The READY shell is **viewport-locked** (`overflow: hidden`; desktop `h/maxH: 100vh`, mobile `h/maxH: 100dvh` with footer inside the flex column) so that flex budget is real — exclusive pages and the broadcast stage own their own internal scroll (`customScroll`). Page bodies use vertical pad only (`pv`) — no extra horizontal `p` / `ph` on exclusive pages. Floating overlay owns matching horizontal pad (`ph` = `page.padY`, same token as page `pv`).

Exclusive scroll owners (Init, Live waiting, and any future exclusive page under the locked shell): `flx_1` + `minH={0}` + `overflowY: auto` + `customScroll` on org `iconSecondary`. Use `ai_c` for horizontal centering — **never** Flex `center` on the scroll owner (`justifyContent: center` clips the top when content overflows).

## In-shell page (`MeetingPage`)

`MeetingLayout` nests `MeetingPageProvider` under live/session providers (`useState<MeetingPage>("init")`) — same provider chain style as the other Meeting providers.

| Piece | Role |
|---|---|
| `MeetingPage` (in `useMeetingPage.tsx`) | Page id union including `"init"` |
| `MeetingDrawerPage` (local in `MeetingDrawerPanel`) | `Exclude<MeetingPage, "init">` for tile ids |
| `MeetingPageProvider` / `useMeetingPage` | Shared `page` / `setPage` for drawer + page children |
| `Meeting.tsx` | Owns the page switch — which component mounts for `page` |
| `meeting/pages/*` | In-shell page bodies (`MeetingInitPage` lobby; `MeetingLivePage` room; `MeetingAttendancePage`; other drawer ids may still be `MeetingPageStub`) |

- One `Meeting` route only — local state, not URL segments.
- Drawer tiles call `setPage`; they do not own page rendering.
- Non-init pages may be title-only stubs (`MeetingPageStub`) until product UI ships under `meeting/pages/` — except `"attendance"`, `"live"`, `"agenda"`, and `"talkQueue"`, which are shipped product UI.
- Do not put in-shell bodies under `ui/pages/` (route entries only). Do not invent a second route for a drawer target.

## MeetingInitPage (`"init"`)

- Shared for every `me.type`. Org logo/name + live `subject` / type / status when present.
- Type/status chips: shared `MeetingMetaChip` (not a private page `MetaChip`; not customer `MeetingMetaChips`).
- Attend UI: colocated `InitAttendSection` in the same file (page-only). Prefer a real component over nested ternaries or `render*` helpers for multi-branch page sections.
- Attend: `can.attend` → `MeetingPrimaryButton` → `actions.attend` + caption `attendRequiresForRoom` under the button (no direct proxy / `batch` writes).
- Before the open window (`!attendWindow.windowOpen`): remaining-duration `attendAvailableIn` + `attendRequiresForRoom`. If the window is already open but `can.attend` is still false, show `attendRequiresForRoom` only — never a past “available in” duration. No disabled fake attend button.
- Attended: confirmation strip + first-person relative `attendedAt` + quieter `roomUnlockedHint` (`textTertiary`). Left: `leftTitle`.
- End meeting UI: colocated `InitEndMeetingSection` in the same file. `can.endMeeting` → `MeetingPrimaryButton` → `await confirm(..., "danger")` → `actions.endMeeting()` (no direct emit / `batch`). Do not put an end-meeting CTA on broadcast or Meeting room waiting UI.
- Colors only from `OrganizationColors` / `defaultOrganizationColors()`.
- Drawer `DrawerGridItem` optional accessible override prop is named `ariaLabel` (repo convention — not `a11yLabel`).

## Placement (shared vs page-local)

- **Shared under `meeting/`** when chrome is reusable across shell pages (e.g. `MeetingPrimaryButton`, `MeetingMetaChip`, `MeetingLiveBroadcast`, `MeetingPageOverlay`).
- **Colocated in the page file** when the UI is owned by one in-shell page only (e.g. `InitAttendSection`, `InitEndMeetingSection`). Use a named component, not an inline `renderFoo()` helper.
- Do not invent a second shared file for page-only sections.

## Meeting room (`"live"`)

- Bounce to `"init"` when `!can.enterLive` (same pattern as locked attendance).
- Waiting UI only (pre-start): title + `waitingLead`; when `can.startMeeting` → `MeetingPrimaryButton` → `actions.startMeeting`.
- After `meeting.status === "STARTED"`, `Meeting.tsx` keeps `MeetingLiveBroadcast` mounted under the content stack; `"live"` shows broadcast only (no overlay). Do not mount broadcast inside `MeetingLivePage`.
- Do not call `useMeetingLive` / `batch` / `useMeetingAttendWindow` from this page.

## Persistent broadcast + page overlay (`STARTED`)

- Owner: `Meeting.tsx` `MeetingContent` — not the drawer, not individual pages.
- Before `STARTED`: exclusive `renderPage(page)` (unchanged).
- While `STARTED`: always mount `MeetingLiveBroadcast`; when `page !== "live"`, wrap `renderPage(page)` in `MeetingPageOverlay` (frosted inset sheet; dismiss → `setPage("live")` via close, outside tap, or Meeting room tile).
- `MeetingPageOverlay`: org `pageOverlayBackground` (fixed `"rgba(229,235,245,0.9) rgba(6,12,22,0.9)"`) + `inputBorder` + `card.radius` + `backdrop-filter: blur(4px)` — not solid `pageBackground`. Floating page bodies use `bg="@transparent"` so frost shows through. Overlay root `z` must stay above the broadcast floating action bar (`z={3}` over bar `z={2}`).
- Full content-column width (`l`/`r` 0 — same `FlexContainer` as header/footer); vertical sheet inset for float; scroll body `ph={page.padY}` so floating page content matches page `pv`; floating close over content (compact theme/lang chrome, `semanticColor.input*`) — no reserved top pad that defeats floating.
- When `STARTED && page === "live" && !can.enterLive`: effect bounces to `"init"`.
- Broadcast **internals** (LiveKit room, track attach, camera/mic/sound controls, mute-all) are out of scope here: `.cursor/rules/website-meeting-livekit-broadcast.mdc`.

## Agenda (`"agenda"`)

- All roles. Data: `hooks/useMeetingAgenda.ts` from session `meeting.agendaItems` (no GQL, no direct `batch`).
- Active = `status === "DISCUSSING"` (no root pointer). Split: discussing strip + other list (`sortOrder`). Heading `otherSection` only when discussing exists **and** other rows remain.
- Card: `MeetingAgendaCard.tsx` — sort badge, full subject (no `maxLi`), read-only status pill **or** chair chips.
- Chair: pass `statusOptions` / `onSetStatus` only when `can.setAgendaItemStatus`; write via `actions.setAgendaItemStatus` (live map only).
- Page copy: single translator `meetingLayout.agenda`. Page `bg="@transparent"`.
- Drawer: when any item is `DISCUSSING`, agenda tile `livePulse` + `agendaDiscussingAria` (def-owned; drawer translator only).

## MeetingPrimaryButton

- Shared org primary full-width CTA under `meeting/MeetingPrimaryButton.tsx`.
- Props: resolved `label` / `ariaLabel` / `onClick` only — reads org colors via `useOrganization` (no `colors` prop).
- Do not re-copy Init/Live button chrome inline; do not use `FormActionButton` for meeting-shell org fills (that control uses fixed `semanticColor`).

## MeetingMetaChip

- Shared org type/status chip under `meeting/MeetingMetaChip.tsx`.
- Props: resolved `label` + `tone` `"brand" | "accent"` only — reads org colors via `useOrganization`.
- Do not keep a private `MetaChip` inside Init (or other pages) when this shared chip exists.
- Distinct from customer-portal `MeetingMetaChips` (plural) under `customer/meetings/` — different surface, different tokens.

## Attendance log (`"attendance"`)

- Chairperson-only. Page owns bounce: non-chair → `setPage("init")` + render `null` (same pattern as locked `MeetingLivePage`). Data hook has no navigation side effects.
- Quorum strip only when `typeof meeting.minMembersCount === "number"` and `> 0` (hide when missing on older BLOBs — no invented default). Numerator = `CHAIRPERSON` \| `MEMBER` with bucket `present` via `countQuorumPresent`.
- Helpers + data: `hooks/useMeetingAttendance.ts` (pure bucket/sort/quorum helpers live in the same file as the hook). Filters UI: shared presentational `FilterCountChips` — page passes resolved options from the hook; no meeting translator inside the chip group. Card: `MeetingAttendanceCard.tsx` — own chrome (not `MeetingHeaderMe`).
- Page copy: single translator `meetingLayout.attendance` (incl. `title`). Do not mix `drawer` translator for the page title.
- Buckets / filter ids: `present` \| `awaiting` \| `left` (sort present → awaiting → left). Card prop: `present`.
- Absent card: `idleCardBackground` + `subtleDivider`. Present card: `presentCardBackground` + check disc (`accentActionBackground` + `accentActionText`). Type chip stays `sectionAccentBackground` (must not equal the present card fill).
- `idleCardBackground` / `presentCardBackground` live on `OrganizationColors` in `useOrganization` — do not branch `colorScheme` or reuse `sectionAccentBackground` for the present card body.
- Card rows come from the hook (`statusLabel` / `present` ready). Type label resolved inside the card from `header.type*`.
- Empty participants: `emptyParticipants` + `subtitle`. Empty filter: `emptyFilter` only (Empty default description).

## Drawer tiles (role IA)

Role from `useMeetingLiveSession().me?.type` (`CHAIRPERSON` vs other).

| Role | Tiles (order) |
|---|---|
| Chairperson | `live`, Meeting info (`init`), `talkQueue`, `attendance`, `agenda`, `decisionsAndVote` |
| Member / Viewer | `live`, Meeting info (`init`), `agenda`, `decisionsAndVote` |

- **No** `participants` tile.
- Meeting info is full-row after `live` (`itemHome` + `HomeMark`; `setPage("init")`; selected when `page === "init"`). Label: **Meeting info** / **معلومات الاجتماع**. Not above the grid; not an `init` id inside `DrawerGridItemDef[]`.
- `live` is full-row (`wide` → `gridColumn: 1 / -1`). Label copy: **Meeting room** / **غرفة الاجتماع** (`itemLive`); id stays `"live"`.
- Put `disabled` / `livePulse` / `ariaLabel` on the **tile def**, not computed inside `flatMap`. Drawer uses **only** `meetingLayout.drawer` translator (`liveLockedAria`, `liveBroadcastingAria`, `agendaDiscussingAria`, `talkQueueActiveAria`, `decisionsVotingAria`) — do not pull `init` / `agenda` / `talkQueue` / `decisions` namespaces into the drawer.
- When `meeting.status === "STARTED"` and `can.enterLive`, `live` tile `livePulse` (corner broadcast ping) — independent of selected chrome.
- When `!can.enterLive`, `live` tile `disabled` (`opc` 0.55, `aria-disabled`; `ariaLabel` = `drawer.liveLockedAria`). Keep the visible Meeting room label. `MeetingLivePage` also bounces to `"init"` if reached while locked.
- When any `agendaItems[*].status === "DISCUSSING"`, `agenda` tile `livePulse` + `agendaDiscussingAria`.
- When any `participants[*].talkTurn != null`, chair `talkQueue` tile `livePulse` + `talkQueueActiveAria`.
- When any `DURING` decision is `UNDER_VOTING`, `decisionsAndVote` tile `livePulse` + `decisionsVotingAria` (one shared tile def for both role lists — do not duplicate the literal per list).
- Meeting info control: full-row after `live` (`itemHome` + shared `HomeMark` with white `frameClr`/`accentClr` via `actionIconOnFill`, same as drawer-menu monochrome accent override). Do not use `FiHome` or leave HomeMark orange accent on the fill well.
- Tiles call `setPage(id)` (and `onClose` on the mobile overlay); selected tile: soft `sectionAccentBackground`, partial-height start accent rail (`3px`, inset top/bottom), `textAccent` label; icon well stays `primaryActionBackground` + white `actionIconOnFill` (no selected swap, no full perimeter brand border). `aria-current="page"`.
- Tile chrome matches existing meeting drawer `DrawerGridItem` (org `OrganizationColors`, `semanticDims`).

## Talk queue (`"talkQueue"`)

- Chairperson-only. Page owns bounce: non-chair → `setPage("init")` + render `null` (same pattern as `MeetingAttendancePage`). Data hook has no navigation side effects.
- Data: `hooks/useMeetingTalkQueue.ts` from session `participants[*].talkTurn` + `meeting.currentTalkMemberId` (no GQL, no direct `batch`, **no** SQL `TalkRecord`).
- Writes: `actions.giveTalkFloor` / `removeFromTalkQueue` / `endTalkFloor` only. Non-chair raise-hand belongs to the broadcast row, **not** this page and **not** the header.
- Card: `MeetingTalkQueueCard.tsx`. Give-floor action renders only on the head row (`isHead && canGive`); Remove renders per row on `canRemove`.
- Order and display rules (FIFO, head-only grant, `place` renumbering, no auto-promote): `.cursor/rules/meeting-talk-queue.mdc`.
- Page copy: single translator `meetingLayout.talkQueue`; participant type labels are the only cross-namespace read (`header.type*`, same as attendance/queue cards). Page `bg="@transparent"`.
- Head/floor cards use `presentCardBackground` + `accentActionBackground` border; idle rows `idleCardBackground` + `subtleDivider` (same token pairing as attendance).
- Drawer: when any `participants[*].talkTurn != null`, `talkQueue` tile `livePulse` + `talkQueueActiveAria`. Pulse tracks the **queue**, not the floor.

## Decisions & voting (`"decisionsAndVote"`)

- All roles. Data: `hooks/useMeetingDecisions.ts` from session `meeting.decisions` (no GQL, no direct `batch`, **no** SQL `Vote`). Writes: `actions.castVote` / `setDecisionStatus` / `clearDecisionVotes` only.
- Section order: Current vote panel (open `DURING` ballot) → in-meeting section → pre-start section. `DURING` rows are hidden until `meeting.status === "STARTED"`; each visible section renders `Empty` instead of collapsing.
- Current vote panel reuses the talk-queue floor chrome (`sectionBrandBackground` + `subtleDivider` + `caption` `textBrand` heading) — do not invent a third panel style.
- Page renders only: `canCast` / `canStartVoting` / `canManage` / `adoptStatus` come from the hook. No capability math, no `meeting.status` read, in the page or the card.
- Card: `MeetingDecisionCard.tsx` — sort badge, subject, status pill, meta line, two tally chips, then a `subtleDivider` hairline before the action stack. Cast pair and open-vote pair are filled half buttons; the recorded vote is the same pair rendered read-only (`disabled` + `disabledStyle` + `aria-pressed`); adopt / revote / cancel are transparent text buttons with icons.
- Page copy: single translator `meetingLayout.decisions`. Page `bg="@transparent"`, sections `gap={2}`.
- Drawer: when any `DURING` decision is `UNDER_VOTING`, `decisionsAndVote` tile `livePulse` + `decisionsVotingAria` (def-owned, shared by both role lists).
- Ballot rules (phase gates, one open ballot, final cast, majority settle, tally visibility): `.cursor/rules/meeting-decisions-vote.mdc`.

## Header identity

- Current participant cluster: `MeetingHeaderMe` (avatar + name + type chip) when `me` exists; props from session `me.name` / `me.type` / `me.avatarUrl` + org `colors`; trailing push (`pushTrailing`).
- Do **not** import `customer/IdentityAvatar` into meeting shell — compose the avatar circle with org `primaryActionBackground` and `actionIconOnFill` for the empty glyph.
- Type copy: `header.typeChairperson` / `typeMember` / `typeViewer` (`CHAIRPERSON` / `VIEWER` / else member). Visible text only — no redundant group `aria-label`.
- Name: `name.trim()` only (no invented empty-name placeholder). Cluster `maxW` uses numeric rem (`maxW={14}`), not `"14rem"`.
- **No talk control in the header.** Non-chair raise-hand lives on the broadcast action row (`.cursor/rules/website-meeting-livekit-broadcast.mdc`); the chair administers the queue on the `talkQueue` page. Do not reintroduce `header.requestTalk` / `requestTalkAria` or a disabled mic placeholder.

## Colors / Utils

- Prefer `OrganizationColors` tokens from `useOrganization` / `defaultOrganizationColors()` — including scheme pairs like `idleCardBackground` / `presentCardBackground` and fixed overlay fill `pageOverlayBackground`.
- **Create a token — do not patch.** If light/dark need different fills, or two surfaces collide visually, add a `ColorType` pair on `OrganizationColors` in `useOrganization.ts`. Forbidden: `colorScheme` / `useThemeManager` branches for meeting chrome fills; opacity hacks on the whole card; reusing `sectionAccentBackground` for a present card body when the type chip already uses it.
- Floating overlay sheet uses `pageOverlayBackground` (fixed rgba pair at 0.9 alpha) — do not reuse `pageBackground` for the frost sheet. Exclusive floating pages keep `bg="@transparent"`.
- Solid action wells (drawer icon squares, selected filter count on primary): glyph ink is `actionIconOnFill` (fixed `#FFFFFF`), not luminance `primaryActionText`. Accent-filled check discs use luminance `accentActionText`.
- Light-scheme `sectionBrandBackground` / `sectionAccentBackground`: `softLight` mix default **0.78** in `useOrganization` (readable chips on `pageBackground`). Do not wash toward 0.92; do not hardcode chip fills in page code. Present attendance card fill uses `presentCardBackground` (`softLight` **0.62** / `softDark` **0.18**).
- `As={"button"}`: `baseCssStyle` = `...ElementStyles.buttonReset` only when every state sets `bg` (incl. `"@transparent"`). Use `background: "transparent"` in `baseCssStyle` **only** with `bg={selected ? fill : undefined}`. `fontVariantNumeric` / overflow scroll live in `cssStyle`.
- Menu glyph may keep `semanticColor.iconPrimary` on `DrawerMenuIcon` (shared with customer shell mark).
- Meeting Home `HomeMark`: pass `frameClr` + `accentClr` = `actionIconOnFill` so the orange floor rail does not appear on the primary fill well.
