---
description: Registration invariants for socket namespaces, controllers, middlewares, rooms, and emit paths in the Ejtmaa backend
globs: backend/src/resources/configs/socket/**/*.ts,backend/src/app/socket/**/*.ts,backend/src/app/notify/**/*.ts,backend/src/resources/consts/NotificationsConsts.ts
alwaysApply: false
---

# Socket namespace registration

Authoritative reference: `docs/platforms/backend/modules/nodejs-socket-library.md` §3, §10.

## Single registration point

`backend/src/resources/configs/socket/io.ts` owns namespaces, controllers, middlewares, and routes.
A controller class is inert until it is registered there, and a namespace key is the literal
Socket.IO path (`/customer`, `/supervisor`, `/meeting`).

Required per namespace:

- `globalMiddlewares` — `auth` for actor namespaces; `meeting_auth` for `/meeting`
- `routes.connection` pointing at a registered controller alias

## Rooms and namespaces are constants

Namespace paths, FCM namespaces, and room names come from
`backend/src/resources/consts/NotificationsConsts.ts`. Never inline a room string in a controller or
notify event. Meeting uses `Namespaces.MEETING`, `FCM_Namespaces.MEETING`, `Rooms.MEETING(meetingId)`.

## Connection controllers own the room join and child bind set

Joining rooms happens in the `connection` controller after the namespace middleware has populated
`socket.data`. Refuse the connection by throwing when the expected context is missing —
`MeetingConnectionIOController` uses `currentMeeting(socket, true)` as the reference shape.

The connection `handle()` return array is the absolute set of child event aliases to bind.
`/meeting` returns `["meeting.live.sync", "meeting.live.update"]`. Meeting controllers live under
`backend/src/app/socket/controllers/meeting/`, share `MeetingIOControllerBase` (which owns that
tuple), and register as dotted `/meeting` routes (`"meeting.live.sync": "meeting_live_sync"`).
A child handler that returns `[]` unbinds those aliases until the next connection bind.

## Emitting

The library has no emit helper. Broadcast through `@nodejs/notify` event classes under
`backend/src/app/notify/events/`, declaring `channels()` as `{ namespace, room }`.

Room emit is the only working outbound path. Targeted single-socket emit (`channel.id`) is inert
because `driver.socketsMap` is never populated — the broadcaster lookup itself was fixed in
`@nodejs/notify` `7.0.0`. Prefer a dedicated room over reviving it: rooms already handle multi-device
clients and clean themselves up on disconnect, while the map needs a manual writer plus manual
deletion.

## Mirroring

User-facing events must stay mirrored into the frontends — see
`.cursor/rules/socket-event-mirroring.mdc`.
