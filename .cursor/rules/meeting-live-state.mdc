---
description: Meeting live CRDT state — Yjs V2 codec on every hop, live_state BLOB never in GraphQL, registry-owned document, columns reflected only in an explicit step
globs:
  - backend/src/app/helpers/MeetingLiveDocHelper.ts
  - backend/src/app/orm/models/Meeting.ts
  - backend/src/app/socket/controllers/meeting/**/*
  - backend/src/app/gql/bridges/customer/MeetingBridge.ts
  - website/src/app/ui/components/meeting/**/*
alwaysApply: false
---

# Meeting live state (CRDT)

The collaborative document behind `meeting.live.*`. Transport rules are in `.cursor/rules/meeting-realtime-socket.mdc`.

Authority: `docs/platforms/backend/contracts/meeting-live-state.md`, `docs/invariants/backend.md` B25.

## Non-negotiables

1. **V2 everywhere.** `encodeStateAsUpdateV2` / `applyUpdateV2` on the BLOB, the sync reply, and the broadcast. A V1 update from a local doc event is converted with `convertUpdateFormatV1ToV2` before it is emitted. Mixing codecs throws at runtime.
2. **`yjs` stays pinned and identical** in `backend/package.json` and `website/package.json`; `@syncedstore/*` is pinned too.
3. **`live_state` never reaches GraphQL.** `MeetingBridge` keeps `registerOrmAttrs = { expect: ["live_state"] }`. Any ORM column that must not be exposed goes in that exclusion list — auto-registration is the default.
4. **Codec + seed live only in `MeetingLiveDocHelper`.** Private helpers (`createLiveDoc` / `encodeLiveDoc` / `decodeLiveDoc` / `buildLiveParticipants` / `buildLiveAgendaItems` / `buildLiveDecisions` / `getLiveDoc`). `Meeting` ORM owns the `live_state` column only — no `LIVE_*` statics or instance live helpers on the model.
5. **The registry owns the document.** Read or create it through `getOrCreateMeetingLiveDoc(meetingId)`; never build a second `Y.Doc` for a meeting that may already be live, and never write `live_state` outside `MeetingLiveDocHelper`.
6. **Persistence is a side effect of document updates**, debounced in the helper. Controllers do not call `update()` on the row.
6b. **A lifecycle reset clears both copies.** When the requester invalidates the session state (approve, or demotion of an approved meeting back to draft), `live_state = null` goes in the transaction and `destroyMeetingLiveDoc(meetingId, { flush: false })` runs in `transaction.afterCommit` — never before the commit, and never with the default flush, which would rewrite the BLOB that was just cleared (`meeting-live-state.md` §3.1).
7. **Columns are reflected in one explicit step.** Meeting-row `subject` / `type` / `status` are applied from the document to SQL only in the (still deferred) completion step, transactionally, and never as a side effect of a socket event.
8. **`MEETING_LIVE_MAP` is one shared constant** (mirrored in `types/meeting.ts`). Backend `doc.getMap(MEETING_LIVE_MAP)` and website SyncedStore root key must use it.
9. **SyncedStore root is empty `Partial`.** Shape `{ [MEETING_LIVE_MAP]: Partial<MeetingLiveMap> }`; initializer `{ [MEETING_LIVE_MAP]: {} }` only. Do not nest `participants: {}` / `agendaItems: {}` / `decisions: {}` (or other nested maps) in the SyncedStore initializer — that throws. Consumers read `liveStore[MEETING_LIVE_MAP]`.
10. **Live `participants`, `agendaItems`, `decisions`, and per-decision `votes` are nested `Y.Map`s.** Seed builds nested maps so collaborative field updates merge by identity. A plain object under those keys is not a collaborative map. Per-decision `votes` seeds from SQL `Vote` rows (empty `Y.Map` when none; no non-voter placeholders).
11. **Live map shape is the mirrored `MeetingLiveMap`** in `backend/src/app/types/meeting.ts` ↔ `website/src/types/meeting.ts` (no GQL / ORM / translation imports). SQL and GraphQL keep using `MeetingType` / `MeetingStatus` / `_Meeting*Value`; the CRDT document uses `MeetingLiveMap` only. Changing one mirror without the other is forbidden — see `.cursor/rules/meeting-live-map-mirror.mdc`.
12. **No automatic BLOB schema migration.** Non-empty `live_state` is decoded as-is; missing `participants` / `agendaItems` / `talkTurn` / `currentTalkMemberId` / `decisions` / nested `votes` is not back-filled on load. Requester reset (§3.1) clears the BLOB so the next sync re-seeds.
13. **Agenda session fields:** durable SQL `status` is mirrored into the live map on seed; `isLiveCreated` is map-only. Active agenda line is `status: "DISCUSSING"`. Cancel is `status: "CANCELED"` (no live delete). Do **not** add root `currentAgendaItemId`. Durable agenda lines remain `AgendaItem`. Website chair may flip live `status` via session `setAgendaItemStatus` (no SQL reflect until §6).
14. **Talk-queue session fields stay map-only.** Per-participant `talkTurn` (`null` = not queued) and root `currentTalkMemberId` (`null` = nobody speaking) are not SQL columns; they die with live-document reset. Durable talk history remains `TalkRecord`, and the shipped website talk queue writes **no** `TalkRecord` row. Set `currentTalkMemberId` **after** `participants` in `createLiveDoc`. Client write rules (FIFO, head-only grant, no auto-promote): `.cursor/rules/meeting-talk-queue.mdc`.
15. **Decision session fields:** seed all SQL decisions; nest `votes` from SQL `Vote` (empty when none); `isLiveCreated` map-only; status may be `CANCELED`. DURING active decision is `status: "UNDER_VOTING"` (at most one). Do **not** add root `currentDecisionId`. Do not pre-book vote keys per participant. Durable decisions/votes remain SQL. **Seed normalization:** `buildLiveDecisions` seeds a `PRE_START` row whose SQL status is `NEW` or `UNDER_VOTING` as `UNDER_VOTING` (pre-start ballots have no "open the vote" step); every other row mirrors SQL as-is, and SQL is never written back. Client write rules (phase gates, one open DURING ballot, vote finality, majority settle): `.cursor/rules/meeting-decisions-vote.mdc`.

## Known ceilings (do not "fix" silently)

Debounce with no maximum wait, no flush on shutdown, eviction only on approve/demotion (nothing evicts when a session merely ends), single process, handshake-snapshot status gate, no presence writers yet. All are documented in `meeting-live-state.md` §7 — change them deliberately and update that section, do not patch around them locally.

## Forbidden

- Exposing, logging, or returning the raw BLOB.
- Writing meeting columns from a socket controller.
- Applying a client update without the status gate, or trusting `status` from the document as the lifecycle truth.
- Adding a second in-memory document store beside the helper registry.
- Re-adding live codec/statics onto `Meeting` ORM.
- Live-deleting an agenda map entry for cancel, or adding `isDeleted` on `MeetingLiveAgendaItem` — use `status: "CANCELED"`.
- Adding root `currentAgendaItemId` or `currentDecisionId` — active markers are status values (`DISCUSSING` / `UNDER_VOTING`).
- Seeding `currentTalkMemberId` before `participants` in `createLiveDoc` (set it after the nested participants map).
- Storing plain `{}` as the Yjs value of `votes` under a decision — use an empty nested `Y.Map`.
- Pre-seeding empty vote slots for every participant under `votes`.
