---
description: LiveKit media plane — helper, join-token HTTP, participant token cache (not a LiveKit ORM/room table)
globs: backend/src/app/helpers/LiveKitHelper.ts,backend/src/app/http/controllers/website/custom/MeetingLiveKitTokenController.ts,backend/src/app/http/middlewares/OrganizationHostMiddleware.ts,backend/src/app/orm/models/MeetingParticipant.ts,backend/.env.example,backend/package.json,website/src/app/ui/components/meeting/hooks/useMeetingLiveKitToken.ts,website/src/resources/configs/axios/api.ts,website/src/types/extends/global.ts
alwaysApply: false
---

# LiveKit media plane

Authoritative contract: `docs/platforms/backend/contracts/livekit-media-plane.md`.
Workflow skill: `.cursor/skills/meeting-livekit-token/SKILL.md`.

## Boundaries

- LiveKit = **A/V only**. Do not store agenda, votes, talk queue, or attendance truth in LiveKit. Durable agenda/decision/vote/talk lines stay SQL; session fields live on `MeetingLiveMap` (`meeting-live-state.md`), not on LiveKit.
- **No** LiveKit ORM model / room table / Zoom-style `platform`/`url` columns on `Meeting`.
- Room name: `ejtmaa:meeting:{meetingId}` via `LiveKitHelper.roomName` only.

## Helper

- File: `backend/src/app/helpers/LiveKitHelper.ts`.
- Dependency: `livekit-server-sdk` pinned (currently `2.17.0`).
- Env: `LIVEKIT_URL`, `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET` (prefer dashboard `wss://` URL).
- Default token TTL: `LiveKitHelper.DEFAULT_ACCESS_TOKEN_TTL` (`12h` from mint). Change that constant to retarget; override per call with `options.ttl`. Persist SQL expiry with the same 12h window.
- Return **client** URL from `clientUrl()` / `createAccessToken` (`ws`/`wss`), not a raw `https` host for browser connect.
- Mint with `AccessToken` + `addGrant` + `await toJwt()` (SDK v2 async).
- Room APIs via `LiveKitAPI` → `api.room.*`.

## Ownership

- Helper does **not** own roster/org/`STARTED` authz or SQL attendance writes — `MeetingLiveKitTokenController` (and requesters when used) do.
- Publish grants are LiveKit defaults for every roster member; do not claim participant-type media policy exists until `canPublish` is mapped from `MeetingParticipant.type`.
- Controller file name is **`MeetingLiveKitTokenController`** (not `OrgLiveKitTokenController`) even though the route lives under `OrgCustomRouter` `/custom/org/...`.
- Join-token HTTP: `POST /website/custom/org/livekit_token` with per-route `org_host`; `STARTED` via `peekMeetingLiveDoc` + `readLiveFields` (**no create**). Peek is in-process only.
- `org_host` (`OrganizationHostMiddleware`) requires the org customer’s active subscription (`getCurrentSubscription` → else `MEETING_ACTIVE_SUBSCRIPTION_REQUIRED`) before any controller runs — including token mint/reuse. `/custom/org/start` must not use `org_host`.
- Persist reused JWTs on `MeetingParticipant.livekit_token` / `livekit_token_expires_at` only — never on GQL.
- One JWT per member; never reuse one token across members.
- Response is `{ token, url }`. `url` is recomputed from env via `clientUrl()` on every response — never persisted beside the cached JWT.
- Website hook public API is a union on `status` (`idle|pending|ready|error`): `ready` carries `token` **and** `url`; every other status carries neither. Quiet network retry uses `skipNetworkToast` via `declare module "axios"` in `website/src/types/extends/global.ts` — no Axios casts.
- Browser consumer is `useMeetingLiveKitRoom` → `MeetingLiveBroadcast`; its invariants live in `.cursor/rules/website-meeting-livekit-broadcast.mdc`.
- Do not commit secrets; keep values in gitignored `.env` only.
